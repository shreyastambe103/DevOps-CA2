apiVersion: v1
kind: Service
metadata:
  name: flexibook-frontend-service
spec:
  selector:
    app: flexibook-frontend
  ports:
    - protocol: TCP
      port: 80        # Service port (cluster-wide)
      targetPort: 80  # Container port (from Deployment)
  type: NodePort      # Or LoadBalancer if using cloud
