https://github.com/Abhishek-2502/Ansible

# Testing ansible can access it or not
ansible backend1 -i inventory.ini -m ping
ansible frontend1 -i inventory.ini -m ping
ansible jenkins-master -i inventory.ini -m ping

# secrets.yaml
ansible-vault encrypt secrets.yaml

# Frontend and Backend setup
ansible-playbook -i inventory.ini docker_install.yaml -l backend,frontend
ansible-playbook -i inventory.ini java_install.yaml -l backend,frontend

# Jenkins Master setup
ansible-playbook -i inventory.ini jenkins_master.yaml
ansible-playbook -i inventory.ini jenkins_password.yaml

# Backend setup (with vault password prompt)
ansible-playbook -i inventory.ini docker_install.yaml -l backend --ask-vault-pass
